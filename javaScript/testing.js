// document.write('<h5>Writing in the body of the html page!</h5>');

let num = 4;
switch (num) {
    case 1:
        console.log(`case 1: ${num}`);
        break;
    case 2:
        console.log(`case 2: ${num}`);
        break
    case 3:
        console.log(`case 3: ${num}`);
        break
    default:
        console.log(`Default ${num}`);
}
